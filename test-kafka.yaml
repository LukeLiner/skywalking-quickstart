receivers:
  kafka:
    brokers:
      - kafka:9092
    protocol_version: 2.0.0
    group_id: test-group
    initial_offset: earliest

exporters:
  debug:

service:
  pipelines:
    traces:
      receivers: [kafka]
      exporters: [debug]
