#????
spring:
  application:
    name: xiaozhou-order
  cloud:
    nacos:
      discovery:
        #nacos server address
        server-addr: nacos:8848
        username: nacos
        password: nacos
#????
server:
  port: 8089

management:
  endpoints:
    web:
      exposure:
        include: 'prometheus'
  metrics:
    export:
      prometheus:
        enabled: true
    tags:
      application: ${spring.application.name}